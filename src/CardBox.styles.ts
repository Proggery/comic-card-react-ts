import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  align-items: center;

  .react-card-flip {
    width: 100%;
  }

  button {
    opacity: 0;
    border: none;
  }
`;
